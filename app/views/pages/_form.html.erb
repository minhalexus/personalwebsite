<%= form_with(scope: @message, url: message_create_path, local: true) do |form| %>
  <% if @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% @message.errors.full_messages.each do |mess| %>
        <li><%= mess %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= form.text_field :name, class: "form-control", placeholder:"Full Name" %>
  </div>

  <div class="form-group">
    <%= form.text_field :email, class: "form-control", placeholder:"Email" %>
  </div>

  <div class="form-group">
    <%= form.text_area :content, class: "form-control", placeholder:"Your Message", rows:"5" %>
  </div>

  <div class="actions">
    <%= form.submit 'Send Message', class: "btn btn-orange border-none btn-rounded-corner" %>
  </div>

<% end %>


