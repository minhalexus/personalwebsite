<div class="container padding-top-bottom-60">
	<h3 class="center"> <%= @project.Title %> </h3>

	<div class="col-md-8 sm-display-none">
		<% puts "#{@project.Title.downcase.split.last}" %>
		<% if ActionController::Base.helpers.resolve_asset_path("#{@project.Title.downcase.split.last}.jpg") == nil %>
			<%= image_tag @project.image, class: "show-project-image" unless @project.image.nil? %>
		<% else %>
			<% puts "******************************** EXISTS *******************" %>
			<% puts "#{@project.project_image}" %>
			<%= image_tag @project.project_image, class: "show-project-image" unless @project.project_image.nil? %>
		<% end %>
	</div>


	<div class="col-md-8">
		<div class="padding-top-35">
			<p>
			  <strong>Description:</strong>
			  <%= @project.Description %>
			</p>


			<% if (@project.youtube_url != nil && @project.youtube_url != "") %>
				<div class="youtube-video">
					<%= (@project.youtube_url).html_safe %>
				</div>
			<% end %>

			<% if (@project.github_url != nil && @project.github_url != "") %>
				<%= link_to "Checkout Code At Github", @project.github_url %>
			<% end %>
		</div>
	</div>

	<% if logged_in? (:admin) %>
		<%= link_to 'Edit', edit_project_path(@project), class: "btn btn-orange border-none btn-rounded-corner" %> |
	<% end %>
	<%= link_to 'Back', projects_path, class: "btn btn-white border-none btn-rounded-corner"%>
</div>